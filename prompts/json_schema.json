{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "JobHuntingMailAnalysis",
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "enum": ["説明会", "面接", "選考結果", "その他"],
      "description": "The category of the job-hunting email."
    },
    "company": {
      "type": "string",
      "description": "The name of the company sending the email."
    },
    "date": {
      "type": ["string", "null"],
      "description": "Format: YYYY-MM-DD HH:MM if a specific event date is mentioned, otherwise null."
    },
    "summary": {
      "type": "string",
      "maxLength": 100,
      "description": "A brief summary of the email in Japanese."
    },
    "next_action": {
      "type": "string",
      "description": "The next concrete step the candidate needs to take."
    }
  },
  "required": ["type", "company", "date", "summary", "next_action"]
}
